# Site assets

Drop image assets used by the site here (referenced from `../index.html`).

## App Store screenshots
The landing page (`index.html`, "See it in place" section) has three placeholder
tiles. Replace them with real screenshots once produced. Suggested files:

- `shot-connected.png` — home-screen widget, connected (green) state
- `shot-dropped.png` — the moment it drops (connected vs not)
- `shot-provider.png` — provider check result

These are the first three of the six App Store frames specced in
`../../marketing/app-store-listing.md`.

## Other useful assets (optional)
- `icon.png` / `favicon` — app icon for the nav + browser tab
- `app-store-badge.svg` — official "Download on the App Store" badge (from
  Apple's marketing guidelines) once the app is live
- `og-image.png` — 1200×630 social preview image for the `og:image` meta tag
